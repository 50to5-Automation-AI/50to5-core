{
    "initialization_agent": {
      "name": "InitializationAgent",
      "system_message": "Initialize the environment.",
      "redis_config": {
        "host": "localhost",
        "port": 6379
      },
      "logger_name": "InitializationAgent",
      "db_config": {
        "dbname": "agent_db",
        "user": "agent_user",
        "password": "password",
        "host": "localhost",
        "port": "5432"
      }
    },
    "quality_control_agent": {
      "name": "QualityControlAgent",
      "description": "Ensures the quality control and state management of the system.",
      "db_config": {
        "dbname": "agent_db",
        "user": "agent_user",
        "password": "password",
        "host": "localhost",
        "port": "5432"
      }
    }
  }
  